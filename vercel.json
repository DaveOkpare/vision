{
  "buildCommand": "cd ui && npm install && npm run build",
  "outputDirectory": "ui/out",
  "functions": {
    "api/index.py": {
      "runtime": "python3.9"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    }
  ]
}